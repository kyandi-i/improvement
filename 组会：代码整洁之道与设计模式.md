# *代码整洁之道与设计模式*

# **一、目的**

核心目标：明确本次组会核心——通过规范代码编写、掌握基础设计模式、运用工具保障，解决当前代码中“难读懂、难修改、难接手”的问题，确保每位成员都能快速接手项目维护，降低协作成本、减少潜在bug。

议程预告：先讲解代码整洁的基础规范（命名、函数），再介绍3个常用设计模式，接着演示Lint工具配置，最后通过实操演示和运用要求，确保大家学以致用。

# **二、核心内容讲解**

## （一）代码整洁之道：基础规范

核心原则：代码是写给人看的，其次才是给机器执行的；整洁代码能大幅降低维护成本，让接手者快速理解逻辑，减少修改时的风险。重点讲解2个最影响可维护性的基础点：

### **1. 命名规范（核心：清晰、无歧义、符合直觉，不用猜）**

核心要求：见名知意，避免模糊、缩写（通用缩写除外，如url、id），遵循项目统一风格（本项目统一使用Java规范：小驼峰命名变量/函数、大驼峰命名类、全大写下划线分隔常量）。

具体规则+正反例（以Java语言为例，贴合本项目实际）：

- 变量命名：小写开头，动词+名词（布尔值用is/has前缀）

```java
// 反例（模糊、缩写）
boolean flag1; // 无法判断含义
String uN; // 缩写模糊，不知道是userName还是userNickname
// 正例（清晰、规范）
boolean isLogin; // 明确表示“是否登录”
String userName; // 明确表示“用户名”
Integer orderId; // 明确表示“订单ID”
```

- 函数命名：小写开头，动词+动作+对象，明确函数做什么

```java
// 反例（模糊、无明确含义）
public void doSomething() { ... } // 不知道做什么操作
public void handleData() { ... } // 不知道处理什么数据、怎么处理
// 正例（清晰、规范）
public User getUserInfo(Long userId) { ... } // 明确“根据用户ID获取用户信息”
public boolean checkParamValid(User user) { ... } // 明确“校验用户参数合法性”
```

- 类命名：大写开头，名词，体现类的职责

```java
// 反例（模糊、无意义）
public class User { ... } // 模糊，未体现类的作用（是实体类、控制器还是服务类）
public class MyClass { ... } // 无任何意义，接手者无法判断用途
// 正例（清晰、规范）
public class UserController { ... } // 明确“用户控制器”，处理用户相关接口请求
public class OrderService { ... } // 明确“订单服务”，处理订单相关业务逻辑
public class UserDTO { ... } // 明确“用户数据传输对象”，用于接口参数/返回值
```

- 常量命名：全大写，下划线分隔

```java
// 反例（不符合常量规范）
public static final int maxPageSize = 10;
public static final String userStatusActive = "ACTIVE";
// 正例（清晰、规范）
public static final int MAX_PAGE_SIZE = 10; // 明确“最大页码大小”
public static final String USER_STATUS_ACTIVE = "ACTIVE"; // 明确“用户激活状态”
```

注意：统一项目命名风格，后续新增/修改代码必须遵循，避免“一人一种写法”。

### **2. 函数拆分（核心：单一职责，一个函数只做一件事）**

核心问题：部分代码存在“大函数”（几百行代码一个函数），逻辑混乱，修改一处牵一发而动全身，接手者难以梳理逻辑。

**拆分原则：**

- 一个函数只负责一个功能，函数长度控制在50行以内（复杂场景不超过100行）。

- 函数参数不超过3个，超过则封装为对象（避免参数过多，难以理解每个参数的作用）。

- 避免函数内嵌套过深（不超过3层if/for嵌套），嵌套过深可拆分为子函数。

正反例对比（以Java语言，本项目“用户登录校验”功能为例）：

拆分前（大函数，逻辑耦合）：

```java
// 反例：一个函数包含所有登录相关逻辑，超过50行，职责不单一
public String login(String account, String password, String verifyCode) {
    // 1. 校验参数
    if (account == null || account.isEmpty()) {
        return "账号不能为空";
    }
    if (password == null || password.isEmpty()) {
        return "密码不能为空";
    }
    if (verifyCode == null || !verifyCode.equals("123456")) {
        return "验证码错误";
    }
    // 2. 密码加密
    String encryptPwd = DigestUtils.md5DigestAsHex(password.getBytes());
    // 3. 查询用户
    User user = userDao.selectByAccount(account);
    if (user == null) {
        return "账号不存在";
    }
    // 4. 校验密码
    if (!user.getPassword().equals(encryptPwd)) {
        return "密码错误";
    }
    // 5. 生成token
    String token = JwtUtils.generateToken(user.getId());
    return token;
}
```

拆分后（多个小函数，职责单一，便于维护）：

```java
// 正例：拆分后，每个函数只做一件事，长度均不超过50行，接手者可快速理解各步骤逻辑
public String login(String account, String password, String verifyCode) {
    // 调用子函数，逻辑清晰
    String paramMsg = checkLoginParam(account, password, verifyCode);
    if (paramMsg != null) {
        return paramMsg;
    }
    String encryptPwd = encryptPassword(password);
    User user = getUserByAccount(account);
    if (user == null) {
        return "账号不存在";
    }
    if (!checkPassword(user.getPassword(), encryptPwd)) {
        return "密码错误";
    }
    return generateLoginToken(user.getId());
}

// 子函数1：校验登录参数（单一职责：只做参数校验）
private String checkLoginParam(String account, String password, String verifyCode) {
    if (account == null || account.isEmpty()) {
        return "账号不能为空";
    }
    if (password == null || password.isEmpty()) {
        return "密码不能为空";
    }
    if (verifyCode == null || !verifyCode.equals("123456")) {
        return "验证码错误";
    }
    return null;
}

// 子函数2：密码加密（单一职责：只做密码MD5加密）
private String encryptPassword(String password) {
    return DigestUtils.md5DigestAsHex(password.getBytes());
}

// 子函数3：根据账号查询用户（单一职责：只做用户查询）
private User getUserByAccount(String account) {
    return userDao.selectByAccount(account);
}

// 子函数4：校验密码（单一职责：只做密码比对）
private boolean checkPassword(String dbPwd, String encryptPwd) {
    return dbPwd.equals(encryptPwd);
}

// 子函数5：生成登录token（单一职责：只做token生成）
private String generateLoginToken(Long userId) {
    return JwtUtils.generateToken(userId);
}
```

## （二）常用设计模式

核心目的：设计模式是解决“重复代码、逻辑混乱、难以扩展”的成熟方案，掌握这3个常用模式，能规范代码结构，让接手者快速理解模块间的关系，减少维护成本。重点讲解“是什么、适用场景、项目中怎么用”，避免纯理论（以下均以Java语言，贴合本项目实际编写示例）。

### 1. 单例模式

核心思想：一个类只能有一个实例，全局可访问，避免重复创建实例造成资源浪费（如工具类、配置类）。

适用场景：项目中的工具类（如日期工具、加密工具）、配置管理器、数据库连接池。

项目运用（以本项目的DateUtil日期工具类为例，采用懒汉式单例，线程安全）：

```java
// 正例：单例模式实现DateUtil工具类，全局唯一实例
public class DateUtil {
    // 私有构造方法，禁止外部new实例
    private DateUtil() {}

    // 懒汉式：延迟创建实例，加volatile保证线程安全，避免指令重排
    private static volatile DateUtil instance;

    // 全局访问入口，双重检查锁定，确保只创建一个实例
    public static DateUtil getInstance() {
        if (instance == null) {
            synchronized (DateUtil.class) {
                if (instance == null) {
                    instance = new DateUtil();
                }
            }
        }
        return instance;
    }

    // 工具方法：日期转字符串
    public String dateToString(Date date, String pattern) {
        SimpleDateFormat sdf = new SimpleDateFormat(pattern);
        return sdf.format(date);
    }

    // 工具方法：字符串转日期
    public Date stringToDate(String dateStr, String pattern) throws ParseException {
        SimpleDateFormat sdf = new SimpleDateFormat(pattern);
        return sdf.parse(dateStr);
    }
}

// 调用方式（全局唯一实例，避免重复创建）
DateUtil dateUtil = DateUtil.getInstance();
String now = dateUtil.dateToString(new Date(), "yyyy-MM-dd HH:mm:ss");
```

注意：避免滥用单例（如业务类不适合用单例），防止造成代码耦合。

### 2. 观察者模式

核心思想：定义对象间的一对多依赖，当一个对象状态变化时，所有依赖它的对象都会收到通知并自动更新（解耦观察者和被观察者）。

适用场景：事件通知、状态同步（如项目中“用户注册成功后，同步发送短信、添加积分、记录日志”）。

项目运用（结合本项目“用户注册”模块，实现观察者模式）：

```java
// 1. 观察者接口（所有观察者需实现此接口，统一通知方法）
public interface Observer {
    // 通知方法，参数为被观察者传递的消息（这里是注册成功的用户信息）
    void update(User user);
}

// 2. 被观察者（用户注册服务）
public class UserRegisterService {
    // 存储所有观察者（短信、积分、日志）
    private List<Observer> observers = new ArrayList<>();

    // 注册观察者（添加需要同步的操作）
    public void addObserver(Observer observer) {
        observers.add(observer);
    }

    // 移除观察者（无需同步的操作）
    public void removeObserver(Observer observer) {
        observers.remove(observer);
    }

    // 通知所有观察者（用户注册成功后调用）
    private void notifyObservers(User user) {
        for (Observer observer : observers) {
            observer.update(user);
        }
    }

    // 核心业务：用户注册
    public void register(User user) {
        // 1. 执行注册核心逻辑（保存用户到数据库）
        userDao.insert(user);
        System.out.println("用户注册成功，用户名：" + user.getUserName());
        // 2. 通知所有观察者，执行同步操作（解耦，无需在注册方法中写所有同步逻辑）
        notifyObservers(user);
    }
}

// 3. 具体观察者1：发送注册短信
public class SmsObserver implements Observer {
    @Override
    public void update(User user) {
        // 发送短信逻辑（模拟）
        System.out.println("给用户" + user.getPhone() + "发送注册成功短信");
    }
}

// 4. 具体观察者2：给新用户添加积分
public class PointObserver implements Observer {
    @Override
    public void update(User user) {
        // 添加积分逻辑（模拟）
        user.setPoint(100);
        userDao.updatePoint(user);
        System.out.println("给用户" + user.getUserName() + "添加100积分");
    }
}

// 5. 具体观察者3：记录注册日志
public class LogObserver implements Observer {
    @Override
    public void update(User user) {
        // 记录日志逻辑（模拟）
        System.out.println("记录注册日志：用户" + user.getUserName() + "于" + new Date() + "注册成功");
    }
}

// 调用方式
public class Test {
    public static void main(String[] args) {
        // 创建被观察者
        UserRegisterService registerService = new UserRegisterService();
        // 添加观察者（同步操作）
        registerService.addObserver(new SmsObserver());
        registerService.addObserver(new PointObserver());
        registerService.addObserver(new LogObserver());
        // 执行注册，自动触发所有同步操作
        User user = new User(1L, "zhangsan", "13800138000", "123456");
        registerService.register(user);
    }
}
```

### 3. 工厂模式

核心思想：定义一个工厂类，负责创建其他类的实例，隐藏对象创建的细节，统一创建逻辑，便于扩展。

适用场景：对象创建逻辑复杂、需要统一管理，或有多种同类对象（如支付方式：微信支付、支付宝支付）。

项目运用（以本项目的“支付模块”为例，实现工厂模式）：

```java
// 1. 支付接口（所有支付方式需实现此接口，统一支付方法）
public interface Payment {
    // 支付方法，返回支付结果
    String pay(Double amount);
}

// 2. 具体支付方式1：微信支付
public class WechatPayment implements Payment {
    @Override
    public String pay(Double amount) {
        // 微信支付核心逻辑（模拟）
        return "微信支付成功，金额：" + amount + "元";
    }
}

// 3. 具体支付方式2：支付宝支付
public class AlipayPayment implements Payment {
    @Override
    public String pay(Double amount) {
        // 支付宝支付核心逻辑（模拟）
        return "支付宝支付成功，金额：" + amount + "元";
    }
}

// 4. 支付工厂类（统一创建支付实例，隐藏创建细节）
public class PaymentFactory {
    // 根据支付类型，创建对应的支付实例
    public static Payment createPayment(String payType) {
        switch (payType.toLowerCase()) {
            case "wechat":
                return new WechatPayment();
            case "alipay":
                return new AlipayPayment();
            default:
                throw new IllegalArgumentException("不支持的支付方式：" + payType);
        }
    }
}

// 调用方式（演示，无需关心对象创建细节，扩展方便）
public class Test {
    public static void main(String[] args) {
        // 1. 微信支付（只需传入支付类型，工厂创建实例）
        Payment wechatPay = PaymentFactory.createPayment("wechat");
        System.out.println(wechatPay.pay(100.0));

        // 2. 支付宝支付
        Payment alipay = PaymentFactory.createPayment("alipay");
        System.out.println(alipay.pay(200.0));

        // 3. 新增支付方式（如银联）：只需新增UnionPayPayment类，修改工厂类的switch，无需修改原有业务代码
    }
}
```

### 设计模式总结：

不用死记硬背，重点记住“适用场景”，项目中遇到对应问题时，优先使用设计模式解决；避免滥用设计模式，简单逻辑无需过度设计（如一个简单的工具方法，无需用工厂模式）。

## （三）Lint工具配置

核心作用：通过工具自动检查代码规范（命名、缩进、语法错误、冗余代码等），强制规范代码编写，减少人工检查成本，确保团队代码风格统一，降低接手者的理解成本。

具体配置（以本项目Java语言为例，使用CheckStyle工具，结合IDEA集成）：

- 第一步：安装对应Lint工具（IDE中直接安装CheckStyle插件）

```Plain
1. 打开IDEA → File → Settings → Plugins → 搜索“CheckStyle” → 安装并重启IDEA
2. 项目中添加CheckStyle依赖（pom.xml中，Maven项目）
<dependency>
    <groupId>com.puppycrawl.tools</groupId>
    <artifactId>checkstyle</artifactId>
    <version>10.12.1</version>
    <scope>provided</scope>
</dependency>
```

- 第二步：配置规则文件（自定义规则，贴合本次组会规范）

```Plain
// 新建checkstyle.xml文件，放在项目src/main/resources目录下，核心规则如下（贴合命名、函数规范）
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN" "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    // 命名规范：类名大驼峰
    <module name="TypeName">
        <property name="format" value="^[A-Z][a-zA-Z0-9]*$"/>
    </module>
    // 命名规范：变量、函数小驼峰
    <module name="MemberName">
        <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
    </module>
    <module name="MethodName">
        <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
    </module>
    // 命名规范：常量全大写下划线分隔
    <module name="ConstantName">
        <property name="format" value="^[A-Z][A-Z0-9_]*$"/>
    </module>
    // 函数长度：不超过50行
    <module name="MethodLength">
        <property name="max" value="50"/>
    </module>
    // 禁止未使用变量
    <module name="UnusedVariable"/>
</module>
```

- 第三步：集成到IDE（VSCode中配置CheckStyle，开启自动检查和修复）

```Plain
1. 打开VSCode → 点击左侧「扩展」图标（或按Ctrl+Shift+X） → 搜索“CheckStyle for Java” → 安装并重启VSCode
2. 配置规则文件：打开VSCode设置（Ctrl+,），搜索“CheckStyle: Configuration File” → 点击「编辑在settings.json中」
3. 在settings.json中添加配置，指定checkstyle.xml规则文件路径（示例）：
   {
       "checkstyle.configurationFile": "${workspaceFolder}/src/main/resources/checkstyle.xml",
       "checkstyle.enable": true,
       "checkstyle.run": "onSave" // 保存时自动检查
   }
4. 开启自动修复：安装“ESLint”插件（辅助代码修复），打开设置搜索“Editor: Format On Save”，勾选开启，保存时自动修复部分不规范代码（如缩进、命名）
```

```
1. 打开IDEA → File → Settings → Tools → CheckStyle
2. 点击“+”，添加自定义规则文件（选择项目中的checkstyle.xml）
3. 勾选“Enable CheckStyle”，设置“Severity”为Error（不规范代码标红）
4. 开启自动修复：File → Settings → Tools → Actions on Save
   勾选“Run CheckStyle”和“Reformat code”，保存时自动检查并修复部分不规范代码
```



- 第四步：集成到项目构建流程（可选，Maven集成，提交代码前强制检查）

```Plain
// pom.xml中添加插件，执行mvn clean install时自动执行CheckStyle检查
<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-checkstyle-plugin</artifactId>
            <version>3.2.0</version>
            <configuration>
                <configLocation>src/main/resources/checkstyle.xml</configLocation>
                <failOnViolation>true</failOnViolation> // 检查不通过则构建失败
            </configuration>
            <executions>
                <execution>
                    <id>checkstyle</id>
                    <phase>validate</phase>
                    <goals>
                        <goal>check</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
```

注意：配置完成后，团队所有成员统一使用相同的Lint规则，后续新增代码必须通过Lint检查。

### 4. Lint工具

打开不规范代码，演示IDE中Lint工具的提示（如命名错误、冗余代码、缩进问题），点击“自动修复”，查看修复后的效果；演示提交代码时，Lint检查不通过的提示，说明如何修改才能通过检查。

打开不规范代码（如变量uN、函数doSomething），IDE中CheckStyle标红提示，右键点击代码 → CheckStyle → Fix Violations，自动修复部分不规范问题（如变量名修改）；故意保留一个错误（如函数长度超过50行），执行mvn clean install，展示构建失败提示，说明如何拆分函数解决问题。

# 三、运用要求

核心：确保组会后，所有成员能将所学内容运用到实际工作中，真正提升代码可维护性，为项目接手做好准备。

# 四、组会总结与答疑

1. 总结：本次组会重点讲解了代码整洁的基础规范（命名、函数）、3个常用设计模式、Lint工具配置，核心目标是提升代码可维护性，让每位成员都能快速接手项目维护；关键在于“落地执行”，后续需严格按照要求整改和规范代码。
2. 答疑：收集成员提出的疑问（如设计模式的适用场景、Lint配置问题、代码拆分思路），现场解答，确保每个人都能理解并会运用。

